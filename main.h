#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <SDL.h>
#include <SDL_image.h>
#include <cpr/cpr.h>

#include "tiletools.h"

int event_handler(void *userdata, SDL_Event *event);

#endif // MAIN_H
